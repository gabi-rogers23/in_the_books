import React from 'react'
import {useState, useEffect} from 'react'

const Homepage = () => {
    const navigate = useNavigate();

    return(
        <div className="home">front</div>
    )
}

export default Homepage